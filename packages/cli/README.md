# 9routerd CLI

This package provides the global `9routerd` command for running the prebuilt 9Router app.

```bash
npm install -g 9routerd
9routerd start
9routerd status
9routerd log --lines 50
9routerd stop
```

Running `9routerd` without a subcommand is the same as `9routerd start`.
