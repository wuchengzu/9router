# 9router CLI

This package provides the global `9router` command for running the prebuilt 9Router app.

```bash
npm install -g 9router
9router start
9router status
9router log --lines 50
9router stop
```

Running `9router` without a subcommand is the same as `9router start`.
